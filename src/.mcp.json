{
  "mcpServers": {
    "web_search": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-brave-search"],
      "env": {
        "BRAVE_API_KEY": "${BRAVE_API_KEY}"
      },
      "description": "STAGE 1(시장 스캔)과 부트스트랩(concerns·rules 공개 자료 수집)에서 사용하는 웹 검색 MCP. 실행 환경에 BRAVE_API_KEY 환경변수를 설정한다. 다른 검색 제공자로 교체해도 SKILL.md 절차는 동일하게 동작한다(web_search 도구 이름만 유지)."
    }
  }
}
